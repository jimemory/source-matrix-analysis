分析表格 source matrix with component type for AI.xlsx

- 分析不同的`Type L1`，在不同产品中的source 数量，按照以下角度分析

  Family Name	BC Volume(pcs)，这两个维度能看出source
  Plarform	
  Project Position	
  From Factor	
  Sourcing Strategy	

- source 数量，通过count `Part Number(Mandatory)`的数量获取

- 注意`BC Volume` 要避免重复计算 

- 只要有 product 列，就附上以下列：From Factor	Sourcing Strategy	Plarform	Project Position	Dev Type  Generation Portfolio

- 列 `PCR`中有 value 的对应的行，作为 `Source introduced by PCR`；列 `PCR`中 none value 的对应的行，作为 `Source in BC scope`；之前我们 count 的 source 数量作为 `Source Total`

- 在 sheet `PCR统计汇总`

  - 增加Generation Portfolio 维度

  - 在 sheet `PCR统计汇总`分析 Component LP5X 56<44+48 的原因

  - count source 数量的同时，count `Spec(Mandatory)`的个数，为了计算 `Source QTY/Spec`

  - 每个 Gen, 每个 Type L1, 对应的产品数量也 count 一下，作为 column `Prod QTY`


涉及到python，用uv搭建环境，依赖包从镜像下载 -i https://pypi.tuna.tsinghua.edu.cn/simple
每次更新code，都需要更新readme文档保持最新 



问题

- Source QTY/Spec 的计算中，一种思路是对每个产品都计算一次Source QTY/Spec ，还有一种是累加到一个 Gen && 一个 Type L1后，再计算，此时就会面临Source QTY/Spec 对应的分子分母的取值，是否应该去重。

  - 我认为不应该去重，在 sheet `PCR统计汇总`中，提供 2 个 version 的 Source QTY/Spec QTY，一个是去重的count，一个是不去重的。
  -  sheet `PCR统计汇总`    要包含 Spec Total (去重)	Source QTY (去重)   Spec Total (不去重)    Source QTY (不去重) 	Source QTY/Spec (去重/去重)	Source QTY/Spec (不去重/不去重)  
  -  sheet `PCR统计汇总`    再加 2列 Source in BC scope (不去重)     Source in BC scope/Spec (不去重/不去重)     

- 分析visualize raw data.xlsx。对比各个 Type L1的Source in BC scope/Spec (不去重/不去重)	Source QTY/Spec (不去重/不去重) 分别在过去的 Gen 和Gen12 Target 对比，方便理解今年对 multiple source 数量的增长情况。可视化

- **左上**  BC Scope 标准化比率趋势 (3 个 Gen 对比) ---命名为Source in BC scope / Spec，Y 轴 =Source in BC scope / Spec

    **右上**  整体标准化比率趋势 (3 个 Gen 对比)  命名为 Total Source QTY / Spec，Y 轴 =Total Source QTY / Spec

    **左下**  FY2627 实际值 vs Gen12 Target 对比  命名为 Source / Spec Compare BC, with PCR and Gen12 Guidance，Y 轴 =Source QTY / Spec

    **右下**  Multiple Source 增长率 (FY2425 → FY2627)   移除这个图

- 增加一个图，对比过去 Gen 中的Spec Total (去重)	对比 Gen12 SBB Plan。现在visualize raw data 已经更新。风格和

-  帮我修改所有 count source 的逻辑，在一个产品中，对于 column：Part Description (No Need Input,Auto From Windchill)中的前 30 个字符完全一致的2 个单元格，只 count 1. 

  - 我的本意是，比如以下 2 行在一个 family name 中，column: Supplier(Mandatory)是相等的，而Part Description (No Need Input,Auto From Windchill)中的前 30 个字符完全一致的2 个单元格，计算 source 个数，比如Source QTY (不去重)，Source QTY (去重)，Source introduced by PCR，Source in BC scope，Source in BC scope (不去重) 的时候只 count 1. 
    - MEMORY 12GB 56S RMSB3400MB06IVF-5600 M0
      MEMORY 12GB 56S RMSB3400MB06IVF-5600 I0